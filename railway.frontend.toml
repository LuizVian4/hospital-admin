[build]
builder = "DOCKERFILE"
dockerfilePath = "packages/frontend/Dockerfile"
watchPatterns = ["packages/frontend/**", "packages/shared/**", "package.json", "package-lock.json"]

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
